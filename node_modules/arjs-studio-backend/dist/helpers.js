"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.arrayBufferToBase64 = exports.loadImage = void 0;
var loadImage = function (dataURI) {
    var image = new Image();
    var p = new Promise(function (resolve, reject) {
        image.addEventListener('load', function () {
            resolve(image);
        });
        image.addEventListener('error', function () {
            reject('Error loading image');
        });
    });
    image.src = dataURI;
    return p;
};
exports.loadImage = loadImage;
// thanks to https://stackoverflow.com/a/9458996/2270403
// this should be faster, if we ever have the need: https://gist.github.com/jonleighton/958841
var arrayBufferToBase64 = function (buffer) {
    var binary = [];
    var bytes = new Uint8Array(buffer);
    var len = bytes.byteLength;
    for (var i = 0; i < len; i++) {
        binary.push(String.fromCharCode(bytes[i]));
    }
    return btoa(binary.join(''));
};
exports.arrayBufferToBase64 = arrayBufferToBase64;
//# sourceMappingURL=helpers.js.map