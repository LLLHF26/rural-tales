"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PatternMarkerGenerator = void 0;
/// @ts-nocheck
var helpers_ts_1 = require("../../../helpers.ts");
var PatternMarkerGenerator = /** @class */ (function () {
    function PatternMarkerGenerator(dataURI) {
        if (!dataURI.startsWith('data:image'))
            throw new Error('Invalid data URI');
        this.dataURI = dataURI;
    }
    PatternMarkerGenerator.prototype.toPattern = function () {
        return __awaiter(this, void 0, void 0, function () {
            var image, canvas, context, patternString, orientation_1, imageData, channelOffset, y, x, offset, value;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, (0, helpers_ts_1.loadImage)(this.dataURI)];
                    case 1:
                        image = _a.sent();
                        canvas = document.createElement('canvas');
                        context = canvas.getContext('2d');
                        canvas.width = 16;
                        canvas.height = 16;
                        patternString = '';
                        for (orientation_1 = 0; orientation_1 > -2 * Math.PI; orientation_1 -= Math.PI / 2) {
                            context.save();
                            context.clearRect(0, 0, canvas.width, canvas.height);
                            context.translate(canvas.width / 2, canvas.height / 2);
                            context.rotate(orientation_1);
                            context.drawImage(image, -canvas.width / 2, -canvas.height / 2, canvas.width, canvas.height);
                            context.restore();
                            imageData = context.getImageData(0, 0, canvas.width, canvas.height);
                            if (orientation_1 !== 0)
                                patternString += '\n';
                            for (channelOffset = 2; channelOffset >= 0; channelOffset--) {
                                for (y = 0; y < imageData.height; y++) {
                                    for (x = 0; x < imageData.width; x++) {
                                        if (x !== 0)
                                            patternString += ' ';
                                        offset = y * imageData.width * 4 + x * 4 + channelOffset;
                                        value = imageData.data[offset];
                                        patternString += String(value).padStart(3);
                                    }
                                    patternString += '\n';
                                }
                            }
                        }
                        return [2 /*return*/, patternString];
                }
            });
        });
    };
    PatternMarkerGenerator.prototype.toFullMarker = function (ratio, size, color) {
        return __awaiter(this, void 0, void 0, function () {
            var image, whiteMargin, blackMargin, innerMargin, canvas, context;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, (0, helpers_ts_1.loadImage)(this.dataURI)];
                    case 1:
                        image = _a.sent();
                        whiteMargin = 0.1;
                        blackMargin = (1 - 2 * whiteMargin) * ((1 - ratio) / 2);
                        innerMargin = whiteMargin + blackMargin;
                        canvas = document.createElement('canvas');
                        context = canvas.getContext('2d');
                        canvas.width = canvas.height = size;
                        context.fillStyle = 'white';
                        context.fillRect(0, 0, canvas.width, canvas.height);
                        // draw marker border
                        context.fillStyle = color;
                        context.fillRect(whiteMargin * canvas.width, whiteMargin * canvas.height, canvas.width * (1 - 2 * whiteMargin), canvas.height * (1 - 2 * whiteMargin));
                        // fill inner marker with white (for transparent images)
                        context.fillStyle = 'white';
                        context.fillRect(innerMargin * canvas.width, innerMargin * canvas.height, canvas.width * (1 - 2 * innerMargin), canvas.height * (1 - 2 * innerMargin));
                        // draw inner image
                        context.drawImage(image, innerMargin * canvas.width, innerMargin * canvas.height, canvas.width * (1 - 2 * innerMargin), canvas.height * (1 - 2 * innerMargin));
                        return [2 /*return*/, canvas.toDataURL()];
                }
            });
        });
    };
    return PatternMarkerGenerator;
}());
exports.PatternMarkerGenerator = PatternMarkerGenerator;
//# sourceMappingURL=pattern-marker-generator.js.map