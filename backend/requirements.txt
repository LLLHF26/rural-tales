# ============================================================
# 沉浸式乡村文旅剧本创作与智能导览系统 —— Python 依赖
# Python 3.13
# 安装：pip install -r requirements.txt
# ============================================================

# ------------------------- Web 框架 -------------------------
fastapi==0.115.*
uvicorn[standard]==0.34.*
sse-starlette==2.2.*                # SSE 流式响应（AI 剧本生成 + NPC 对话）

# ------------------------- 数据库 -------------------------
sqlalchemy[asyncio]==2.0.*          # ORM
aiomysql==0.2.*                     # MySQL 异步驱动
redis[hiredis]==5.2.*               # Redis 客户端 + 高性能解析器

# ------------------------- 数据校验 -------------------------
pydantic==2.10.*                    # FastAPI 内置，指定版本
pydantic-settings==2.7.*            # 从 .env 加载配置

# ------------------------- 认证与安全 -------------------------
python-jose[cryptography]==3.3.*    # JWT 生成与校验
passlib[bcrypt]==1.7.*              # bcrypt 密码哈希
python-multipart==0.0.*             # 文件上传支持

# ------------------------- HTTP 客户端 -------------------------
httpx==0.28.*                       # 异步 HTTP（调用 AI API、地图 API）
requests==2.32.*                    # 同步 HTTP（OSS SDK 依赖）

# ------------------------- 对象存储 -------------------------
oss2==2.19.*                        # 阿里云 OSS
# cos_python_sdk_v5==1.9.*          # 腾讯云 COS（按需启用）

# ------------------------- 工具 -------------------------
python-dotenv==1.0.*                # .env 文件加载
python-dateutil==2.9.*              # 时间处理
Pillow==11.*                        # 图像处理（AR 标记验证）
opencv-contrib-python==4.13.*       # ArUco 标记生成

# ------------------------- 测试 -------------------------
pytest==8.3.*                       # 测试框架
pytest-asyncio==0.24.*              # 异步测试支持
